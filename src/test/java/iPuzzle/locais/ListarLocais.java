package iPuzzle.locais;

import iPuzzle.BasePage;
import org.openqa.selenium.WebDriver;

public class ListarLocais extends BasePage {
    public ListarLocais(WebDriver navegador) {
        super(navegador);
    }
}
